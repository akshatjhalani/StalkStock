## Stock Price API
This folder contains the source code for the Stock Price API service. The service uses `serverless` framework to deploy.

### Setup
1. To setup the service, please follow the guide to setup `fetcher` and `hourly_fetcher` first. 
2. Run the following command:

```bash
serverless plugin install -n serverless-python-requirements
serverless deploy
```
